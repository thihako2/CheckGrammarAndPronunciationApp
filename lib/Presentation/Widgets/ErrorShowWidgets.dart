import 'package:flutter/material.dart';

class ErrorShower extends StatelessWidget {
  const ErrorShower({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,
    );
  }
}
